{#

This file is part of the Sonata package.

(c) Thomas Rabaix <thomas.rabaix@sonata-project.org>

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.

#}

{#
    The `show` row action.

    `view_link` is the hook an application's CSS and the functional tests select on (PLAN/02 §8).

    `list_action_button_content` still decides between the icon, the text and both; when the text
    is hidden it stays in the accessibility tree rather than disappearing.
#}

{% if admin.hasAccess('show', object) and admin.hasRoute('show') %}
    {% set button_content = adminata_config.getOption('list_action_button_content') %}
    {% set show_icon = button_content in ['icon', 'all'] %}
    {% set show_text = button_content in ['text', 'all'] %}
    {% set recipe = show_text ? 'adm-btn adm-btn-secondary adm-btn-sm' : 'adm-btn-icon adm-btn-icon-show' %}
    <a
        href="{{ admin.generateObjectUrl('show', object, actions.link_parameters|default([])) }}"
        class="{{ recipe }} view_link"
        title="{{ 'action_show'|trans({}, 'AdminataBundle') }}"
    >
        {% if show_icon %}
            <i class="fas fa-eye" aria-hidden="true"></i>
        {% endif %}
        {% if show_text %}
            {{ 'action_show'|trans({}, 'AdminataBundle') }}
        {% else %}
            <span class="sr-only">{{ 'action_show'|trans({}, 'AdminataBundle') }}</span>
        {% endif %}
    </a>
{% endif %}
