# The forked packages keep upstream's formatting so upstream diffs still apply; adminata's own
# templates are Twig, which Prettier does not parse.
/packages/
/vendor/
/node_modules/
/var/
/build/
/PLAN/
/docs/
package-lock.json
composer.lock
*.md
*.twig
*.php

# Byte-for-byte captures of Symfony's YamlReferenceDumper, which pads values into columns.
# Reformatting them would make ConfigContractTest compare against something the dumper cannot
# produce.
/tests/Contract/config-reference/

# Generated by bin/build-visual-findings.mjs, and deliberately one finding per line: it is a debt
# ledger read in diffs, and Prettier would collapse the short lists onto one line each, so striking
# a single entry would rewrite the whole row.
/tests/Visual/support/findings.json

# Generated by bin/build-css-safelist.mjs.
/assets/css/safelist.css
/assets/css/contract.json
/assets/js/__contract__/controllers.json
