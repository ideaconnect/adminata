.. warning::

    **This screen is not yet ported.** adminata still renders it with the Bootstrap markup it
    inherited from Sonata, which its stylesheet no longer styles, so the page works but looks
    unstyled. The feature itself — the controller, the security checks, the routing — is adminata's
    and is tested. See :doc:`/porting-status` for the full list and how to ask for one.
