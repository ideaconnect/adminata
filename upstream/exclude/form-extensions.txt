# form-extensions was merged into the admin bundle (upstream/merged.txt) and no longer
# has a directory of its own, so upstream/sync.sh refuses it. This list is kept because
# upstream/diff.sh still needs one to split its report, and because it records which of the
# upstream paths adminata had already taken over. The paths are still written relative to the
# old package root.
assets/**
src/Bridge/Symfony/Resources/views/**
src/Bridge/Symfony/Resources/public/**
src/Type/BasePickerType.php
src/Bridge/Symfony/DependencyInjection/SonataFormExtension.php
src/Test/AbstractWidgetTestCase.php
package.json
package-lock.json
webpack.config.js
vite.config.js
postcss.config.js
prettier.config.js
.babelrc.js
.eslintrc.js
.stylelintrc.js
.prettierignore
.github/**
Makefile
*.md
phpunit.xml.dist
rector.php
.php-cs-fixer.dist.php
phpstan.neon.dist
phpstan-baseline.neon
composer.json

# Repo scaffolding: a package here is a directory, not a repository, so it carries none of
# this and a sync must not bring it back.
bin/console
.editorconfig
.yamllint
.gitattributes
.gitignore
.readthedocs.yaml
.symfony.bundle.yaml
