# exporter was merged into packages/admin-bundle (upstream/merged.txt) and no longer has a
# directory of its own, so upstream/sync.sh refuses it. This list is kept because upstream/diff.sh
# still needs one to split its report, and because it records which of the upstream paths adminata
# had already taken over: the three source-iterator tests that were written against SQLite and now
# run on MySQL (PLAN/README directive 9). The paths are still written relative to the old package
# root.
tests/Source/DoctrineDBALConnectionSourceIteratorTest.php
tests/Source/DoctrineORMQuerySourceIteratorTest.php
tests/Source/PDOStatementSourceIteratorTest.php
.github/**
Makefile
*.md
phpunit.xml.dist
rector.php
.php-cs-fixer.dist.php
phpstan.neon.dist
phpstan-baseline.neon
composer.json

# Repo scaffolding: a package here is a directory, not a repository, so it carries none of
# this and a sync must not bring it back.
bin/console
.editorconfig
.yamllint
.gitattributes
.gitignore
.readthedocs.yaml
.symfony.bundle.yaml
