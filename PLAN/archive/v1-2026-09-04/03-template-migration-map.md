# 03 — Template migration map

Rule for every template: **path, block names, ids, `sonata-*` hooks, `objectId`, data attributes
and Stimulus attributes are frozen** (document 02); Tailwind utilities are appended after the
legacy tokens; `<td>`/`<th>` cells are styled through CSS selectors so their attribute strings
stay byte-identical. Detailed element-by-element tables with file:line evidence live in
`R/layout-nav.md`, `R/list-datagrid.md`, `R/forms-edit.md`, `R/show-misc.md` and the TailAdmin
recipes in `R/tailadmin-catalog.md` §2. Controller names refer to document 05.

## A. Layouts, navigation, dashboard, blocks (`R/layout-nav.md`)

| Template | Today | Target (TailAdmin source) | Controllers | Contract notes |
|---|---|---|---|---|
| `standard_layout.html.twig` | AdminLTE 2 shell: `body.sonata-bc.skin-*.fixed`, `header.main-header` before `sonata_wrapper`, `aside.main-sidebar`, `div.content-wrapper` | `T/index.html` shell in the **window-scroll** model: `<aside class="sidebar main-sidebar …">` fixed left (290 px / 90 px rail), content column `lg:ml-[290px]`, sticky header (`T/partials/header.html`), overlay (`T/partials/overlay.html`); all 33 blocks and 12 captured blocks kept; `body.fixed` dropped | `sonata-layout` + `sonata-sticky` on `<body>`; `sonata-theme`; `sonata-search-shortcut`; `sonata-dropdown` on add-block, user-block, actions | `<meta name="sonata-config">`, `sonata_sidebar_hide` cookie seeding, `html.no-js`, `admin_lte_skin_class` output on `<body>`, `_skin`/`_use_*` variables; `<html lang dir>` added; `user-scalable=no` removed; `bootlint` block empty; new blocks `sonata_overlay`, `sonata_header_search`, `sonata_top_nav_menu_dark_mode`, `sonata_top_nav_menu_notifications`, `sonata_script_attributes` |
| `ajax_layout.html.twig` | navbar + `container`/`row` | flex/grid; list-mode switcher and `_list_filters_actions` re-rendered with the same controllers as `base_list`; output wrapped in `<div class="sonata-bc">` so compat rules apply in foreign pages | as in `base_list` | keeps `table.sonata-ba-list` and `.sonata-list-table` (the real app parses XHR lists) |
| `empty_layout.html.twig` | blanks header/sidebar, inline style | same overrides; inline style → body class | — | `sonata_header` renders nothing when `logo` and `sonata_nav` are empty (login pages) |
| `Menu/sonata_menu.html.twig` | KnpMenu renderer forcing `treeview`/`active`, `data-widget="tree"` | `T/partials/sidebar.html` groups: `menu-item`, `menu-item-active/inactive`, `menu-item-icon-*`, `menu-dropdown-item`; group header as `<button aria-expanded>`; per-group open-state map; `keep_open` forced open; `on_top` single items; child icon `fa fa-angle-double-right` dropped (owner) | `sonata-menu`; `sonata-layout` `collapseOnly` targets | `.sidebar-menu .dynamic-menu a` (functional `MenuTest`), `treeview`, `treeview-menu`, `keep-open`, `active`, KnpMenu blocks `compressed_root, root, list, children, item, linkElement, spanElement, label`; role gate and translation chain verbatim |
| `Core/add_block.html.twig` | multi-column Bootstrap mega-dropdown, width `column_count*140px` | dropdown panel (`T/partials/header.html` notification panel recipe) with `grid grid-cols-{column_count}`; group headers with icon | `sonata-dropdown` (panel is the `menu` target) | `dropdown_number_groups_per_colums` maths, `role="menuitem"`, `role_admin` gate |
| `Core/user_block.html.twig` | empty; layout wraps in `ul.dropdown-menu.dropdown-user` if non-empty | TailAdmin user dropdown (avatar initials from `app.user.userIdentifier`) containing the same `<ul>` | `sonata-dropdown` | `<li>` pass-through; no hard-coded SonataUser routes |
| `Core/dashboard.html.twig` | `row` + `col-md-{4\|6\|12}` computed widths, masonry not used | `grid grid-cols-12 gap-4 md:gap-6`; `sonata_grid_class` filter maps `col-md-N` (default `col-md-4`) to `col-span` utilities with pass-through | — | `is_granted_affirmative`, `sonata_block_render_event` hooks, block positions top/left/center/right/bottom unchanged |
| `Core/search.html.twig` | `data-masonry` (only masonry use) | page header + CSS grid; `data-masonry` attribute tolerated/ignored | — | `sonata_admin.search` gate; `q` param; `empty_boxes` classes |
| `Core/tab_menu_template.html.twig` | KnpMenu with `nav navbar-nav`, Bootstrap dropdown items | **underline tabs** inside the sticky sub-navbar; dropdown items via `sonata-dropdown`; `divider_prepend/append` as separators | `sonata-dropdown` | `nav navbar-nav` children class merged by `AbstractAdmin::buildTabMenu` kept; `TabMenuTest` expectations updated |
| `Breadcrumb/breadcrumb.html.twig`, `breadcrumb_title.html.twig` | `<li><a>`, `<li class="active"><span>` | **unchanged files**; layout `<ol>` gets TailAdmin classes; chevrons via CSS `::after`; `aria-label="Breadcrumb"`, `aria-current` added in layout | — | `BreadcrumbsRuntimeTest` pins the markup |
| `Block/block_admin_list.html.twig` | AdminLTE `box` per group + table of admins + `btn-group` actions | TailAdmin card (`rounded-2xl border border-gray-200 bg-white dark:border-gray-800 dark:bg-white/[0.03]`) with a rows list; actions as icon buttons | — | `extends sonata_block.templates.block_base`; inner blocks kept; `dashboard__action*.html.twig` includes |
| `Block/block_stats.html.twig` | `small-box {color}` | metric card (`T/partials/metric-group`); `color` passed through **and** mapped (`bg-aqua`→blue-light, `bg-green`→success, `bg-red`→error, `bg-yellow`→warning) | — | settings defaults and template path asserted by `AdminStatsBlockServiceTest` |
| `Block/block_admin_preview.html.twig` | box + list table | card + list table via `outer_list_rows_{listMode}` | — | inner blocks `list_header, table_header, table_body, table_footer, no_result_content` |
| `Block/block_rss_dashboard.html.twig`, `block_search_result.html.twig` | `box-solid`, `label label-primary`, `nav-stacked` | card with header (icon, title, count badge, create/list icon buttons), chips as light badges, rows list | — | `sonata-search-result-{show,hide,fade}`, `.matches a.label.label-primary` tokens kept |
| Flash messages (new `FlashMessage/render.html.twig`, included from `notice`) | twig-extensions Bootstrap alerts + CSS-checkbox collapse | `T/partials/alert/*` recipes + dismiss button; read-more toggle CSS-only | `sonata-dismiss` | `alert alert-{type}` and `read-more-*` classes; `sonata_flash_*` type map; config switch to fall back to `@SonataTwig` |
| Login page (new `Security/login.html.twig`, optional) | cookbook recipe with AdminLTE `login-box` | `T/src/signin.html` port on `empty_layout`; compat CSS for `login-page`, `login-box*`, `btn-flat` keeps the recipe rendering | — | tier depends on owner decision (SonataUserBundle scope) |

## B. List, datagrid, filters, pager, batch, inline edit, mosaic (`R/list-datagrid.md`)

| Template | Today | Target | Controllers | Contract notes |
|---|---|---|---|---|
| `CRUD/base_list.html.twig` (390 lines) | title/actions, filter dropdown + collapsible filter panel (`form-inline`, select2 operator/value pairs), `table.sonata-ba-list` in `box-body.table-responsive`, batch checkbox column, per-row `btn-group`, export dropdown, pager | Card with header; filter panel as a card whose rows are `grid grid-cols-12 gap-3 items-center` (label 3 / operator 4 / value 4 / remove 1); table wrapper `max-w-full overflow-x-auto` (`T/partials/table/table-06.html`); footer (batch select + submit, export dropdown, per-page, pager) rendered **outside** the overflow wrapper; new `list_after_table` block | `sonata-batch` on the form, `sonata-dropdown` on export, `sonata-filter`/`sonata-filter-list` unchanged, `sonata-select` on value selects when `use_select2` | 22 blocks; `batch_javascript` block kept but empty; ids `filter-*`, `list_batch_checkbox`, `{uniqid}_all_elements`; `name="action"`, `idx[]`, `all_elements`, `_sonata_csrf_token`; `prepareSubmit` needs real `<select name="filter[...]">` (never hidden-input widgets) |
| `CRUD/base_list_field.html.twig` | `<td class="sonata-ba-list-field sonata-ba-list-field-{type}" objectId>` + readmore + x-editable span | **cell attributes byte-identical**; padding/typography via `.sonata-ba-list td.sonata-ba-list-field` CSS; sort header `th > a` with SVG arrows | `sonata-readmore` unchanged; `sonata-editable` on `span.x-editable` | `field`, `field_span_attributes` blocks; `RenderElementRuntimeTest` envelope |
| `list_*.html.twig` (14 typed) + `display_*.html.twig` | `label label-success\|danger` for booleans; plain markup otherwise | badges: keep `label label-*` tokens + `adm-badge adm-badge-{success,error}` utilities; `list_boolean` `data-source` → JSON | — | 20 badge/x-editable test expectations re-baselined |
| `list__action.html.twig`, `list__action_{edit,show,delete,history}.html.twig` | `a.btn.btn-sm.btn-default.{edit,show,delete,history}_link` in `btn-group` | inline button group (`inline-flex -space-x-px`, ends rounded) with icons; `list_action_button_content` honoured | — | ORM/Mongo `ListBuilder`s hard-code these paths; `*_link` classes kept |
| `list__batch.html.twig`, `list__select.html.twig` | checkbox + iCheck | native `appearance-none` checkbox recipe (`TN` `Checkbox.tsx`) | `sonata-batch` (shift-range, select-all with indeterminate) | `sonata-ba-list-field-batch`, `sonata-ba-list-row-selected` |
| `list_outer_rows_list.html.twig`, `base_list_inner_row.html.twig`, `base_list_flat_*`, `list_inner_row.html.twig` | `<tr>` templates | unchanged structure; hover/selected row classes | — | `inner_list_row` key; `row_class`; ajax `_actions` dropped |
| `list_outer_rows_mosaic.html.twig` | `col-xs-6 col-sm-3` tiles, checkbox inside the `<a>` | `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6`; media card with hover overlay; id as primary badge; checkbox moved to the tile footer (D21) | `sonata-batch` | `mosaic-*` classes, `sonata_mosaic_{background,default_view,hover_view,description}` blocks, `mosaic_background` option, `<tr><td colspan>` wrapper kept |
| `CRUD/tree.html.twig` | deprecated (includes a missing `list_tab_menu`) | parity: untouched apart from classes; not rendered by default | `sonata-treeview` (for `ul.js-treeview` users) | D37 |
| `Pager/base_links.html.twig` | `ul.pagination` first/prev/window/next/last | `TN/components/tables/Pagination.tsx` classes (bordered prev/next, `w-10 h-10 rounded-lg` numbers, `bg-brand-500` active) on the **same** `ul.pagination > li(.active) > a` structure; Sonata's `getLinks()` window kept; `aria-label`s | — | `pager_links` option |
| `Pager/base_results.html.twig`, `simple_pager_results.html.twig`, `links.html.twig`, `results.html.twig` | `select.per-page` with URL option values | native select recipe (`w-auto`), URL values unchanged | `sonata-per-page` unchanged; `sonata-select` only when `use_select2` (must preserve option values) | `num_pages`, `num_results`, `max_per_page` blocks; compiler-pass swap to `simple_pager_results` |
| `Form/filter_admin_fields.html.twig` (6 blocks) | `form_div_layout` + `sonata_type_filter_*` rows | operator selects native (styled chevron), value inputs TailAdmin recipes, native `type=date` styled like `T/partials/datepicker.html`; additive `sonata_type_date_range_widget` (side-by-side) | `sonata-select` on value selects; `sonata-autocomplete` via included autocomplete template | block prefixes `sonata_type_filter_{default,choice,number,date,datetime,date_range,datetime_range}`, operator types; ORM/Mongo overrides extend this file |
| `CRUD/batch_confirmation.html.twig` | box page | centred card with danger accent, same POST fields | — | page, not modal (D32) |
| `CRUD/Association/list_{many_to_many,many_to_one,one_to_many,one_to_one}.html.twig` | `sonata-link-identifier` links | badges/links | — | `relation_link`, `relation_value` blocks; ORM `ListBuilder` fallbacks |
| Inline edit (span in `base_list_field`) | x-editable popover, `Admin.setup_xeditable` | Tailwind popover (dropdown-panel recipe) with input/select/textarea/checklist/date + save/cancel; POST to `data-url` with `X-Requested-With`; replaces the closest `<td>` with returned HTML | `sonata-editable` | `sonata_xeditable_type`/`_choices`, `x_editable_type_mapping` parameter, `SetObjectFieldValueAction` untouched |

## C. Forms, edit chrome, associations, autocomplete, collections (`R/forms-edit.md`)

| Template | Today | Target | Controllers | Contract notes |
|---|---|---|---|---|
| `Form/form_admin_fields.html.twig` (695 lines, 30 blocks, extends `form_div_layout`) | Bootstrap row/label/widget/help/errors; `form-horizontal` `col-sm-3/9`; iCheck; select2 | Same block set; row keeps `id="sonata-ba-field-container-{id}"` + `form-group has-error sonata-ba-field …`; widgets get `adm-input/adm-select/adm-textarea/adm-checkbox/adm-radio` (`T/src/form-elements.html`, `TR/components/form/*`); horizontal mode = `grid grid-cols-12` with label `col-span-3`, field `col-span-9`, offset `col-start-4` **plus** the legacy `col-sm-*` tokens; `tailwind_2_layout` variable convention adopted (`row_class`, `widget_class`, `label_class`, `help_class`, `error_item_class`) | `sonata-select` (`choice_widget_collapsed`), `sonata-collection` (unchanged), `sonata-choice-field-mask`, `sonata-association` + `sonata-modal` (`sonata_type_model_list_widget`) | see document 06 §1 for the non-overridden `form_div_layout` blocks; `AdminLayoutTest` rewritten; `help-block` kept as dual class |
| `Form/Type/sonata_type_model_autocomplete.html.twig` (12 KB) | select2 ajax script with 4 overridable JS blocks | `<select id={id}_autocomplete_input data-sonata-select2="false">` + hidden inputs unchanged; Tom Select remote (`load` with pagination, `render.option`), created-object auto-select via `sonata-association:created` event instead of `$(document).ajaxSuccess` | `sonata-autocomplete`, `sonata-association` (mode autocomplete), `sonata-modal` | blocks `sonata_type_model_autocomplete_{widget,ajax_request_parameters,dropdown_item_format,selection_format}` kept; `..._select2_options_js` kept as a deprecated no-op receiving a Tom Select options object; request/response contract unchanged |
| `CRUD/base_edit.html.twig`, `edit.html.twig`, `base_edit_form.html.twig` (16 blocks), `base_edit_form_macro.html.twig` | `nav-tabs-custom`, `box box-primary` groups in `row` + `col-md-12`, `well.form-actions` sticky bar | underline tabs (`role="tablist"`, error badge on tabs with `has-errors`), cards per group via `sonata_box_class` + `sonata_grid_class` (PHP defaults untouched), sticky bottom action bar (`sticky bottom-0 … backdrop-blur`, `.stuck` shadow) | `sonata-confirm-exit`, `sonata-edit` (one-line change), `sonata-tabs`, `sonata-sticky` `action` target, `sonata-dismiss` | `btn_*` names, `_tab` param, `changer-tab`, `tab-pane`, `sonata-ba-form-actions form-actions`, `sonata-ba-collapsed-fields` |
| `CRUD/select_subclass.html.twig` | `btn btn-app btn-block` tiles | tile cards (`rounded-2xl border … hover:border-brand-500`) | — | `sonata_grid_class` |
| `CRUD/base_array_macro.html.twig`, `Helper/render_form_dismissable_errors.html.twig` | `alert alert-danger` + `data-dismiss` | error alert recipe (`T/partials/alert/alert-error.html`) | `sonata-dismiss` | `alert alert-danger`, `list-unstyled` |
| `CRUD/Association/edit_modal.html.twig` | Bootstrap `.modal.fade > .modal-dialog.modal-lg > .modal-content` | `<dialog id="field_dialog_{id}" class="modal adm-modal">` with `.modal-content/.modal-title/.modal-body` inside; sizes lg/md/sm/list; scrollable panel | `sonata-modal` | always included (also from the model-list widget) |
| `edit_many_script.html.twig` (22 KB), `edit_one_script.html.twig` | per-field jQuery: `$.ajax`, `ajaxSubmit`, `.modal()`, violation injection, select2 refresh | **thin shims**: define the five global `onclick` entry functions that forward to the `sonata-association` controller; all logic in JS | `sonata-association` | include points kept; contracts in document 06 §2 |
| `edit_{many_to_many,many_to_one,one_to_many,one_to_one}.html.twig` | `field-container`, `field-actions`, `btn btn-success btn-sm sonata-ba-action` (add/list/edit/delete) | same structure; buttons as `adm-btn adm-btn-sm` with icons; `onclick` shim + `data-action` both present (owner) | `sonata-association` on `#field_container_{id}`; `sonata-sortable` when sortable | `title="Add new"`, `#field_widget_{id}`, `#field_actions_{id}`, `field-short-description`; ORM/Mongo include these by path |
| `edit_one_to_many_inline_table.html.twig`, `_inline_tabs.html.twig` | `table.table-bordered`, `tbody.sonata-ba-tbody`, `td.sonata-ba-td-{id}-{field}`, tabs | TailAdmin table + underline tabs; `_delete` column | `sonata-inline-row`, `sonata-tabs` | `sonata-ba-field-inline-table` hook |
| `edit_one_to_many_sortable_script_{table,tabs}.html.twig` | jQuery UI sortable + `sonata.add_element` listener | **empty files** (kept as include points); behaviour in `sonata-sortable` | `sonata-sortable` | `sonata-ba-sortable-handler` handle; `sonata.add_element` dispatched |
| form-extensions `datepicker.html.twig` (new adminata override `Form/datepicker.html.twig` + theme blocks) | Tempus Dominus `input-group date` + `fas fa-calendar` | flatpickr input recipe (`T/partials/datepicker.html`, theme in `T/css/style.css:407-535`), range linking via `minDate/maxDate` | `sonata-datepicker` | see document 06 §4 |

## D. Show, history, ACL, delete, preview, buttons, helpers (`R/show-misc.md`)

| Template | Today | Target | Controllers | Contract notes |
|---|---|---|---|---|
| `CRUD/base_show.html.twig`, `show.html.twig`, `base_show_field.html.twig` | `sonata-ba-view` > tabs > `row` > `col-md-12` > `box box-primary` > `table.table` rows `tr.sonata-ba-view-container > th/td` | cards via `sonata_box_class`/`sonata_grid_class`; underline tabs; **row stays `<table><tr><th><td>`** styled as TailAdmin table; readmore kept | `sonata-tabs`, `sonata-readmore` | blocks `name, field, field_value, field_compare`; `td:contains` functional assertion; `_tab` logic shared macro |
| `show_*.html.twig` (15), `display_*.html.twig` (12), `Association/show_*.html.twig` (4) | plain values, `label label-*` booleans, `fa-check/fa-times` | same structure; badges dual-classed; currency/percent stay plain concatenation (tests pin `EUR 10.746135`) | — | SHOW_TEMPLATES paths via DI; SonataIntl overrides remain possible |
| `base_show_compare.html.twig`, `show_compare.html.twig` | `tr.history-audit-compare`, `th.diff` pink | same rows; `th.diff` → `bg-warning-50 dark:bg-warning-500/15` | — | `th.diff` pinned by tests |
| `base_history.html.twig`, `history.html.twig`, `history_revision_timestamp.html.twig` | `col-md-5` `table#revisions` + `col-md-7 #revision-detail`, ajax revision loading | `grid lg:grid-cols-12` (5/7); revisions table with current row highlight; detail pane | `sonata-revision` unchanged | ids/classes kept; `ajax_layout` `show` block for XHR |
| `base_acl.html.twig`, `base_acl_macro.html.twig`, `acl.html.twig` | `form-horizontal`, `box box-success`, matrix table, header row every 10 rows, iCheck | TailAdmin table with sticky first column and header, native checkboxes with `aria-label`s, same forms/fields | — | `acl_users_form`, `acl_roles_form`, `btn_update_acl`, `td_role`/`td_username` keys |
| `delete.html.twig` | `sonata-ba-delete` > `box box-danger` | centred `max-w-[600px]` card with the danger-alert body (`TN` alert modal recipe), danger + bordered buttons | — | `_method=DELETE`, `_sonata_csrf_token`, "Yes, delete", "or edit" link |
| `preview.html.twig` | extends `edit`, legacy flat table `tr.sonata-ba-view-title`, hides form via `fieldset/.tabbable` CSS | same page; form body gets a stable class hidden under `.sonata-preview-form-container` | — | `btn_preview_approve/decline`, blocks `preview, form, formactions, actions` |
| `action.html.twig`, `action_buttons.html.twig`, `Button/*.html.twig` (6) | `<li><a class="sonata-action-element"><i class="fas fa-*">` in `ul.nav.navbar-nav.navbar-right` | same `<li><a>` structure; anchors styled by descendant selectors as `adm-btn`; "2+ → Actions dropdown" heuristic kept | `sonata-dropdown` | user `configureActionButtons` templates emit the same markup |
| `dashboard__action.html.twig`, `dashboard__action_create.html.twig` | `btn btn-link btn-flat`, Bootstrap dropdown for subclasses | link buttons; subclass dropdown | `sonata-dropdown` | `action_create` key; icons via `parse_icon` |
| `Helper/short-object-description.html.twig` | `inner-field-short-description` | unchanged | — | JSON contract of `GetShortObjectDescriptionAction` |

## E. Sibling templates that render inside adminata pages

| Source | Handling |
|---|---|
| `TW/…/FlashMessage/render.html.twig` | replaced by adminata's copy (D33); compat CSS keeps the original rendering if an app includes it directly |
| `FE/…/Form/datepicker.html.twig` | blocks overridden in adminata's form theme (D17); standalone `@SonataAdmin/Form/datepicker.html.twig` theme offered for non-admin forms |
| `ORM/…/Block/block_audit.html.twig` (`box`, `panel-group`, `data-toggle="collapse"`) | compat CSS + data-API delegate; upstream PR later |
| `BB/…/Block/block_core_rss.html.twig` (`panel panel-default`, `media`) and the other 11 block-bundle templates | compat CSS (`panel*`, `media`) |
| SonataUserBundle `user_block` / login templates (not readable offline) | compat CSS for `user-header`, `bg-light-blue`, `btn-flat`, `login-box*`; verify template list online |
| SonataIntlBundle `history_revision_timestamp` / `show_*` overrides | unaffected (they replace typed templates by path) |

## F. New or removed files

- New: `Form/datepicker.html.twig` (standalone theme), `FlashMessage/render.html.twig`,
  `Core/list_mode_buttons.html.twig` (shared by `standard_layout` and `ajax_layout`),
  `Core/tabs_macro.html.twig` (shared `_tab` logic), optional `Security/login.html.twig`.
- Emptied but kept: `edit_one_to_many_sortable_script_{table,tabs}.html.twig`; `bootlint` block.
- Removed public assets: `admin-lte-skins/*.min.css` **contents replaced** by token files (paths
  kept), `select2-locale/`, `moment-locale/`, eot/ttf/woff/svg font variants, Source Sans Pro.
- Kept public assets: `images/ajax-loader.gif`, `images/default_mosaic_image.png`,
  `images/logo_title.png` (neutral adminata logo replaces the Sonata one).
